name: Release Notification
on:
  release:
    types: [published]

jobs:
  notify-slack:
    runs-on: ubuntu-latest
    steps:
      - name: Process release body
        id: process-body
        run: |
          # Replace markdown headers and add newlines
          PROCESSED_BODY=$(echo "${{ github.event.release.body }}" | sed -E 's/### (.*)/\n*\\1*:/g; s/## (.*)/\n*\\1*:/g; s/^-\s*/- /g')

          # Output the processed body to GITHUB_OUTPUT
          echo "processed_body=$PROCESSED_BODY" >> $GITHUB_OUTPUT

      - name: Send Slack Notification
        uses: tokorom/action-slack-incoming-webhook@main
        env:
          INCOMING_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          text: "A release is published: ${{ github.event.release.tag_name }}"
          blocks: |
            [
              {
                "type": "header",
                "text": {
                  "type": "plain_text",
                  "text": "${{ github.event.release.tag_name }} is published!"
                }
              },
              {
                "type": "divider"
              },
              {
                "type": "section",
                "fields": [
                  {
                    "type": "mrkdwn",
                    "text": "*Author:*\n${{ github.event.release.author.login }}"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*Published At:*\n${{ github.event.release.published_at }}"
                  }
                ]
              },
              {
                "type": "section",
                "text": {
                  "type": "plain_text",
                  "text": "*Release Notes:*\n${{ steps.process-body.outputs.processed_body }}"
                }
              },
              {
                "type": "actions",
                "elements": [
                  {
                    "type": "button",
                    "text": {
                      "type": "plain_text",
                      "text": "View Release",
                      "emoji": true
                    },
                    "url": "${{ github.event.release.html_url }}"
                  }
                ]
              }
            ]
